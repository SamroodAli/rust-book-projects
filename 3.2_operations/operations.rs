fn main() {
    // addition
    let sum = 5 + 10;

    // subtraction
    let difference = 95.5 - 4.3;

    // multiplication
    let product = 4 * 30;

    // division
    let quotient = 56.7 / 32.2; //1.7608695652173911

    // integer division truncates toward zero to the nearest integer (rounds down).
    let rounded_down = 18 / 5 // 3 instead of 3.6;

    // remainder
    let remainder = 43 % 5; // 3

    println!("{sum}, {difference}, {product} {quotient} {truncated} {rounded_down} {remainder}");
}
